Classify the dominant intent of a pull request into exactly one category: feat (new internal or user functionality), fix (correct a fault), perf (speed or memory optimization), style (readability without semantic change), refactor (structural maintainability excluding perf/style), docs (non-code documentation/comments/docstrings), test (test suite), build (build system/dependencies/configuration), ci (CI scripts/workflows), chore (other maintenance).
Use substantive changed behavior, not title keywords or line count. For mixed intent, explain the dominant choice. Cite changed paths. Return category, rationale, scope, evidence paths and confidence 0..1. If evidence is inadequate return category null, confidence 0 and explain why. Never invent evidence. Respond in Spanish except the category code.
